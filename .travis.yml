language: node_js
node_js:
  - '0.10'
  - '0.8'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-dropwizard' ]; then cd .. && eval "mv $currentfolder generator-angular-dropwizard" && cd generator-angular-dropwizard; fi

